# UX side-by-side: iec run-lifecycle vs guest SoS operator

Walk of [iec-proto-c `docs/ux/journeys/run_lifecycle_monitoring.md`](https://github.com/guypayeur/iec-proto-c/blob/main/docs/ux/journeys/run_lifecycle_monitoring.md) (iec `@4d5d44d`) against this guest’s operator UI (`GET /`, `GET /ui`) and jobs API. Named iec baseline remains [`grammar/examples/reserve_ifrs17`](https://github.com/guypayeur/iec-proto-c) (runtime [`proofs/fixtures/iec-parity/method.yaml`](https://github.com/guypayeur/panoramix-runtime/blob/main/proofs/fixtures/iec-parity/method.yaml) after [#82](https://github.com/guypayeur/panoramix-runtime/pull/82)).

**Honesty:** this note does **not** stamp [runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70) UX Done, does **not** set `north_star_done`, and does **not** unlock cloud [#61](https://github.com/guypayeur/panoramix-runtime/issues/61) / [#29](https://github.com/guypayeur/panoramix-runtime/issues/29). [#78](https://github.com/guypayeur/panoramix-runtime/issues/78) stays open. Guest `demo: reserve` is a stub / UX seed — **not** IFRS17 math.

## Links

| Surface | Where |
|---|---|
| iec journey | [`docs/ux/journeys/run_lifecycle_monitoring.md`](https://github.com/guypayeur/iec-proto-c/blob/main/docs/ux/journeys/run_lifecycle_monitoring.md) |
| Guest operator UI | `GET /` and `GET /ui` (stdlib HTML/JS, no SPA framework) |
| Guest jobs API | `POST/GET /v0/jobs`, `GET /v0/jobs/{id}`, `POST /v0/jobs/{id}/cancel`, `POST /v0/jobs/{id}/pause`, `POST /v0/jobs/{id}/resume`, `POST /v0/jobs/{id}/re-admit` |
| Progress (thinner) | `GET /v0/jobs/{id}/progress` |
| Event trail (thinner) | `GET /v0/jobs/{id}/events` (durable JSONL when hooked; else process-memory; also `events` on the job resource). Filter by kind (`?kind=admit,StageCompleted,pause,…`). Export JSON or JSONL (`?format=jsonl`) for local salvage — **not** SIEM / **not** regulatory defensibility |
| Historical compare (thinner) | `GET /v0/jobs/{id}/compare` — recent same-catalog (or same kind/class) jobs in guest history (in-process plus local lab files when persisted); elapsed prefers durable `wall_elapsed_ms` when the hook or a persisted job field includes it (runtime tip [`9b6646e8`](https://github.com/guypayeur/panoramix-runtime/commit/9b6646e8829056c22339842321986afaf7d6b957) / [PR #114](https://github.com/guypayeur/panoramix-runtime/pull/114) / **main**; docs tip [`6511cec7`](https://github.com/guypayeur/panoramix-runtime/commit/6511cec7) / [PR #116](https://github.com/guypayeur/panoramix-runtime/pull/116) lineage); else created/updated clocks; typical/ETA only from ≥2 succeeded priors with real walls — omit when missing; never invent |
| History persistence (thinner) | Local lab files under `.sos/jobs` (or `PANORAMIX_SOS_JOBS_DIR`); reload recent records on start so compare / recoverability survive guest restart. Optional durable `wall_elapsed_ms` is persisted when seen so priors can use it after restart. Fail-closed if disabled. **Not** a SIEM, **not** a six-month audit product, **not** a cross-host DB |
| Failure / terminal (thinner) | Failed/canceled `GET /v0/jobs/{id}` exposes `terminal` (status + message + optional last events / stage) — **not** a SIEM, **not** iec `/v1/audit/events` |
| Recoverability (thinner) | Failed/canceled jobs expose `recoverability` (handoff + payload for operator/ctl re-admit, including after restart when history was persisted). When a durable hook is active (`PANORAMIX_CTL_HTTP` preferred or `PANORAMIX_RUNTIME_ROOT`), `POST /v0/jobs/{id}/re-admit` is a **new** admit of that handoff/payload. Fail-closed without hook or when payload is missing (no silent stub). Cancel/fail does **not** auto-retry. Pause/resume remains durable-only |
| Handoff docs (thinner) | Job-detail compact reminders for `GET /v0/jobs/{id}/handoff` + `/payload`, recoverability / re-admit, and [`docs/lab-compose.md`](lab-compose.md). Operator clarity — **not** a second control plane |
| Ctl handoff | `GET /v0/jobs/{id}/handoff`, `GET /v0/jobs/{id}/payload` |
| Durable ctl (temporal-local) | operator/ctl `runtime.apply reserve-temporal` admit\|status\|progress\|events\|cancel\|pause\|resume on panoramix-runtime **main** (`progress --id` → `stage` / `stages_total` / `stages_completed` / `fraction` / nested `progress`, verified @ `63c4d8e`; optional `stages[].elapsed_ms` verified @ `9ba95bbb` / docs tip `5dc191cb` (PR #112) / main — omit when missing, never invent; optional `wall_elapsed_ms` / `started_at` verified @ `9b6646e8` / main (PR #114) — omit when missing, never invent; not a forecast; not IFRS17; not iec SPA; `events --id` → JSONL under `.runtime/reserve-temporal/events/`, verified @ `d86552e`; `pause\|resume` verified @ `3a164cd`; status `paused` + `events` / `events_n` / `events_durable`) + runtime [`bindings/local-reserve-temporal.example.yaml`](https://github.com/guypayeur/panoramix-runtime/blob/main/bindings/local-reserve-temporal.example.yaml) (pin 0.5). Same verbs on operator loopback ctl HTTP (runtime **main** @ `fb901542`; `runtime.serve` / `publish.ctl_port` **19215**: `POST /reserve-temporal/admit`, `GET /reserve-temporal/status\|progress\|events`, `POST /reserve-temporal/pause\|resume\|cancel`). Guest may opt in via `PANORAMIX_CTL_HTTP` (preferred) or `PANORAMIX_RUNTIME_ROOT` (subprocess) on the existing hook seam — not guest→mesh ctl. One-shot lab: [`docs/lab-compose.md`](lab-compose.md) / `scripts/lab_compose_reserve_temporal.py` (serve + `CTL_HTTP` + recorded reserve + admit → cancel/fail → `POST /v0/jobs/{id}/re-admit` new job id; `--dry-run` in CI covers hooked + fail-closed without Temporal; `pack_fill.fragment` assists runtime [#78](https://github.com/guypayeur/panoramix-runtime/issues/78) **D** fill — `tips` when known, optional `durable` only when measured from the hooked run; documented runtime tip `b81130f` / [PR #120](https://github.com/guypayeur/panoramix-runtime/pull/120) / **main** (`python3 -m runtime.iec_parity_pack skeleton|validate`); docs tip `63a168d` / [PR #122](https://github.com/guypayeur/panoramix-runtime/pull/122) WSL assist-smoke stamp lineage; gap-report tip `84cb202` / [PR #124](https://github.com/guypayeur/panoramix-runtime/pull/124); guest emit tip `b859466` / [PR #52](https://github.com/guypayeur/panoramix-guest-sos/pull/52); guest gap-report tip `eb48605` / [PR #60](https://github.com/guypayeur/panoramix-guest-sos/pull/60); emit fragment can feed optional durable into `skeleton` via `--from-json` / flags when measured; `pack_fill.merge` hints `python3 -m runtime.iec_parity_pack merge … --from-json` (runtime tip `5086d0f` / [PR #128](https://github.com/guypayeur/panoramix-runtime/pull/128) / **main**; merge docs tip `6ecb645` / [PR #130](https://github.com/guypayeur/panoramix-runtime/pull/130) / **main**; does not run merge; merge ≠ fill; merge ≠ Done; assist ≠ Done); `pack_fill.apply_metrics` hints `python3 -m runtime.iec_parity_pack apply-metrics … --from-durable-panoramix` (runtime tip `5d399f7` / [PR #132](https://github.com/guypayeur/panoramix-runtime/pull/132) / **main**; apply-metrics docs tip `3904ee4` / [PR #134](https://github.com/guypayeur/panoramix-runtime/pull/134) / **main**; does not run apply-metrics; does not supply wall numbers; apply-metrics ≠ fill; apply-metrics ≠ Done; assist ≠ Done); `pack_fill.apply_notes` hints `python3 -m runtime.iec_parity_pack apply-notes …` (runtime tip `48a8645` / [PR #136](https://github.com/guypayeur/panoramix-runtime/pull/136) / **main**; does not run apply-notes; does not supply wall numbers; does not invent UX strings; apply-notes ≠ fill; apply-notes ≠ Done; assist ≠ Done); omit when missing; never invent `metrics.wall_time_sec`; wall feature tip remains `9b6646e8`; assist ≠ fill; does **not** write the full live pack; does **not** stamp #70 UX Done / `north_star_done`). Fail-closed without env or when hook/payload is missing (no silent stub; not resume-from-failed). Not SIEM. Not IFRS17. Pin 0.5. Does not stamp `north_star_done` |
| Catalogs | recorded / live / parity (parity-scale) — guest mirrors panoramix-runtime **main** `runtime.reserve.parity_params` / `digest_for` / [`docs/reserve.md`](https://github.com/guypayeur/panoramix-runtime/blob/main/docs/reserve.md) |

## Journey walk

| Journey step | iec touchpoint | Guest support | Notes |
|---|---|---|---|
| 1.1 Job detail + status | SPA → `GET /v1/jobs/{job_id}` | **match** (thinner) | `GET /v0/jobs/{id}` + job detail panel: id, status pill, kind/class/digest (short), created/updated/elapsed, `message`, `local.stage`, `local.backed`. Optional **durable wall** (`wall_elapsed_ms` / `started_at`) appears only when the durable hook progress/status JSON includes it (runtime tip [`9b6646e8`](https://github.com/guypayeur/panoramix-runtime/commit/9b6646e8829056c22339842321986afaf7d6b957) / [PR #114](https://github.com/guypayeur/panoramix-runtime/pull/114) / **main**) — labeled durable wall; **not** a forecast; **not** IFRS17; **not** iec SPA; **omitted when missing; never invented**. Job list can filter by real status (`GET /v0/jobs?status=` queued/running/paused/succeeded/failed/canceled; unknown values 400). Light auto-refresh is opt-in, or on when `jobs.durable_hook.durable_path`; polls list + selected job detail; **stops on terminal**; **no invented progress**. When `PANORAMIX_CTL_HTTP` is set but `runtime.serve` is down, admit/status/progress fail closed with `ctl_http_unreachable` (lab-serve down) — not a hung poll, not pretend durable. Admit that exceeds guest timeout before a running id is `ctl_admit_timeout` (runtime #143; live|parity mid-flight poll) — fail closed, not stub progress, not lab-serve-down. Failed/canceled jobs add a **terminal** summary (`status` + `message` + optional last events / stage) — labeled **not** SIEM / **not** iec `/v1/audit/events`. Historical comparison (`GET /v0/jobs/{id}/compare`) against recent same-catalog (or same kind/class) jobs in guest history (in-process plus local lab files when persisted): elapsed prefers durable `wall_elapsed_ms` when the hook or a persisted job field includes it (runtime tip `9b6646e8` / main, PR #114; docs tip `6511cec7` / PR #116 lineage); else created/updated clocks; typical/ETA only from succeeded prior walls when two or more samples exist. Honest empty / single-prior (no invented numbers). **not a forecast**, **not** IFRS17, **not** iec SPA historical widget. **Not** a full SPA framework |
| History persistence | guest restart wipes in-process priors | **match** (thinner) | Job records under `.sos/jobs` (default) or `PANORAMIX_SOS_JOBS_DIR`. Reload recent history on start so compare can use succeeded priors across restart, including persisted durable `wall_elapsed_ms` when seen. `off` / empty / unwritable **fails closed**. Recoverability still exports handoff + payload when known. Persistence does **not** invent typical/ETA, walls, or SIEM claims. **Not** a six-month audit product, **not** a cross-host DB |
| 1.2 Step/chunk progress | `GET /v1/jobs/{id}/progress` | **match** (thinner) | `GET /v0/jobs/{id}/progress` prefers durable reserve-temporal path-slice counters (`stage`, `stages_total`, `stages_completed`, `fraction`, nested `progress`) when a hook provides them (`source: durable`). Job-detail UI shows a named path-slice timeline (`admit` / `project` / `fold` / `complete`, or hook-provided names) with the same ownership tags as investigate, plus completed vs current vs pending. Optional per-stage elapsed appears only when durable progress exposes real `stages[].elapsed_ms` (or equivalent) and/or durable `GET /v0/jobs/{id}/events` timestamps can be subtracted honestly — **omitted when missing; never invented**. Runtime tip [`9ba95bbb`](https://github.com/guypayeur/panoramix-runtime/commit/9ba95bbb4fa4c9046abee6eac60158e4fd649b90) / docs tip [`5dc191cb`](https://github.com/guypayeur/panoramix-runtime/commit/5dc191cbfb103147c2b9c32a6be3f5b97732994d) (or **main**, [PR #110](https://github.com/guypayeur/panoramix-runtime/pull/110) / [PR #112](https://github.com/guypayeur/panoramix-runtime/pull/112)) can supply durable `stages[].elapsed_ms` on progress; older tips omit it. Optional `wall_elapsed_ms` / `started_at` (or equivalent) appears on job detail / progress only when the durable hook progress/status JSON includes it (runtime tip [`9b6646e8`](https://github.com/guypayeur/panoramix-runtime/commit/9b6646e8829056c22339842321986afaf7d6b957) / [PR #114](https://github.com/guypayeur/panoramix-runtime/pull/114) / **main**) — labeled durable wall; **not** a forecast; **not** IFRS17; **not** iec SPA; **omitted when missing; never invented**. This guest does not invent elapsed or walls. `fraction` / `stages_completed` stay the hook counters (not invented). Stub fallback (`source: stub`) stays “stage i of n” without fake names or fake timings. Unreachable opt-in CTL_HTTP (`source: unreachable`, `ctl_http_unreachable`) fails closed — not durable counters. Operator/ctl: `python3 -m runtime.apply reserve-temporal progress --id cw_…`. This is not iec chunk progress and not iec planner parallelism or SPA progress |
| 2.1 Pause | `POST /v1/jobs/{id}/pause` | **match** (thinner) | Guest `POST /v0/jobs/{id}/pause` on durable-backed jobs (status `paused` on WorkHandoff). Stub-only → **409** `stub_only` (no pretend). Operator/ctl: `python3 -m runtime.apply reserve-temporal pause --id cw_…`. Cancel is not pause |
| 2.2 Investigate | SPA progress + catalog + Slack | **match** (thinner) | Catalog identity already on the job (name + short digest) called out for cross-check — **not** a data-catalog product. Static day-one step-ownership tags on durable path-slices when hooked (`admit` / `project` / `fold` / `complete` → `ctl / admit`, `kernel / project`, `kernel / fold`, `ctl / complete`; **not** Slack, **not** a live team directory). Event trail stays on the same panel (durable JSONL when hooked; labeled **not** SIEM). Still **not** iec SPA / Slack / real data catalog |
| 3.1a Resume | `POST /v1/jobs/{id}/resume` | **match** (thinner) | Guest `POST /v0/jobs/{id}/resume` on paused durable-backed jobs (status back to `running`). Stub-only → **409** `stub_only`. Operator/ctl: `python3 -m runtime.apply reserve-temporal resume --id cw_…` |
| 3.1b Cancel | `POST /v1/jobs/{id}/cancel` | **match** (thinner) | Guest `POST /v0/jobs/{id}/cancel` on durable-backed jobs signals runtime cancel first (same honesty as pause; `PANORAMIX_CTL_HTTP` preferred or `PANORAMIX_RUNTIME_ROOT`), then marks `canceled` if still live — or follows hook `status()` when ctl already reports terminal. Stub-only stays local cancel; inert default unchanged (fail-closed without hook). Confirm dialog: cancel is not pause; durable cancel is ctl-mediated; cancel/fail does **not** auto-retry. Operator/ctl: `python3 -m runtime.apply reserve-temporal cancel --id cw_…` (workflow cancel). Job detail / list refresh prefers hook `status()` when `runtime_ref` exists (paused / running / terminal from ctl, not a stale stub clock). Not #70 Done |
| Failure surface (clarity) | job detail after fail/cancel | **match** (thinner) | Failed and canceled job detail (API `terminal` + operator UI panel): status + message + optional last events / stage when known. Labeled **not** SIEM / **not** iec `/v1/audit/events` product. No invented wall times or SIEM claims |
| Recoverability (handoff re-admit) | ctl re-admit after fail/cancel | **match** (thinner) | Easy access to `GET /v0/jobs/{id}/handoff` + `/payload` for operator/ctl re-admit (`python3 -m runtime.apply reserve-temporal admit --handoff JSON`). When a durable hook is active, one-click `POST /v0/jobs/{id}/re-admit` posts that handoff/payload through the same hook seam as a **new** admit (new job id). Fail-closed without hook or when payload is missing (`409 re_admit_unavailable`; no silent stub). Lab compose ([`docs/lab-compose.md`](lab-compose.md)) smokes admit → cancel/fail → re-admit → new job id when `PANORAMIX_CTL_HTTP` / `PANORAMIX_RUNTIME_ROOT` is set; `--dry-run` covers hooked + fail-closed without Temporal. Job detail also shows a compact **Handoff docs** panel (export reminders + recoverability / re-admit + lab-compose) for operator/actuary clarity — **not** a second control plane. Copy: cancel/fail does **not** auto-retry; re-admit is a new admit; no resume-from-failed; **not** SIEM; **not** IFRS17. Pause/resume remains durable-only (stub **409** `stub_only`). Salvageable is handoff identity + payload bytes when known — not in-flight compute |
| 4.1 Audit trail | `GET /v1/audit/events?job_id=…` | **match** (thinner) | `GET /v0/jobs/{id}/events` prefers durable reserve-temporal JSONL (`source: durable`, `events_durable`, `events_n`) when a hook provides it. Operator/ctl: `python3 -m runtime.apply reserve-temporal events --id cw_…` (append-only JSONL under `.runtime/reserve-temporal/events/`; kinds admit / StageCompleted / pause / resume / cancel / succeed/fail). Process-memory fallback (`source: memory`, local event trail) otherwise. Filter by kind (`?kind=admit,StageCompleted,pause,resume,cancel,succeed,fail`) on API and operator UI; empty match stays empty. Export/download the current (filtered) trail as JSON or JSONL (`?format=jsonl`) for local salvage. Day-one local durable trail — **not a SIEM**, **not** iec `/v1/audit/events` product, **not** six-month regulatory defensibility |

Support key: **match** = same operator intent, thinner surface; **partial** = honest subset; **missing** = not implemented and not faked.

## What matches today

- Submit → list → get → status pills (`queued` → `running` ⇄ `paused` → `succeeded` \| `failed` \| `canceled`). `paused` is non-terminal. Job list filter is those real statuses only (`GET /v0/jobs?status=`). Light auto-refresh (opt-in checkbox, or on when `durable_hook` is active) polls list + selected job detail and **stops on terminal**. No invented progress. Event trail filter-by-kind + JSON/JSONL salvage export are thinner (labeled **not** SIEM / **not** regulatory defensibility).
- Job detail for a selected run (identity, timestamps, current message/stage, stub vs runtime `backed`). Optional durable wall (`wall_elapsed_ms` / `started_at` from runtime tip `9b6646e8` / main, PR #114; docs tip `6511cec7` / PR #116 lineage) only when the hook progress/status JSON includes it — labeled **not** a forecast / **not** IFRS17 / **not** iec SPA; omitted when missing. Failed/canceled jobs surface a **terminal** summary (status + message + optional last events / stage) — labeled **not** SIEM / **not** iec audit product. Recoverability (thinner): handoff + payload export for operator/ctl re-admit (survives guest restart when history is persisted); one-click `POST .../re-admit` when a durable hook is active (new admit; fail-closed without hook or payload); cancel/fail does **not** auto-retry; no resume-from-failed. Compact **Handoff docs** on job detail remind operators of those exports, re-admit, and lab-compose — not a second control plane. Progress prefers durable path-slice counters when a hook provides them, with a named timeline (`admit` / `project` / `fold` / `complete` or hook-provided) plus investigate ownership tags and completed vs current vs pending; optional per-stage elapsed only when durable progress/events provide real timestamps (omit when missing; never invent); otherwise stub “stage i of n” without fake names. Events prefer durable JSONL when a hook provides them; otherwise process-memory. Historical comparison (thinner) against recent same-catalog / same-kind-class jobs in guest history (in-process plus local lab files): elapsed prefers durable `wall_elapsed_ms` when present (else created/updated); typical/ETA only from real succeeded prior walls. History persistence (thinner): `.sos/jobs` / `PANORAMIX_SOS_JOBS_DIR`; optional wall persisted when seen; fail-closed if disabled; **not** a SIEM. Investigate (thinner): catalog name + short digest called out for cross-check; static path-slice ownership tags when hooked; event trail on the same panel.
- Cancel of a live run with clear framing and `canceled` (one L). Cancel is not pause. Durable cancel is **ctl-mediated**. Stub-backed cancel is **guest local**. When a durable hook is active (`PANORAMIX_CTL_HTTP` preferred or `PANORAMIX_RUNTIME_ROOT`), guest `POST .../cancel` signals runtime cancel first (same honesty as pause), then marks the guest job terminal — or follows hook `status()` when ctl already reports terminal. Job detail / list refresh prefers hook `status()` when `runtime_ref` exists so Temporal-backed runs show paused / running / terminal from ctl, not a stale stub clock. Terminal cancel is **409**. Fail-closed without hook (inert default unchanged). Cancel/fail does **not** auto-retry. Not guest→ctl HTTP over the mesh. Not #70 Done.
- Pause/Resume on the **durable path** (injected hook or opt-in lab adapter): guest `POST .../pause` → `paused`, `POST .../resume` → `running`, `pause_resume: true` on those responses. Stub-only jobs **409** `stub_only`; UI controls stay disabled with an explanation. Operator/ctl: `python3 -m runtime.apply reserve-temporal pause --id cw_…` and `python3 -m runtime.apply reserve-temporal resume --id cw_…` (or the same verbs on loopback ctl HTTP when `PANORAMIX_CTL_HTTP` is set). Operator UI badge (from `GET /v0/info` `jobs.durable_hook`) says the durable path is active **only** when the HTTP adapter is hooked — no pretend when stub. One-shot lab compose: [`docs/lab-compose.md`](lab-compose.md).
- Opaque WorkHandoff emit (`kind` / `class` / `payload_digest` + `id` / `status`) and payload export for the ctl path. Failed/canceled recoverability points at the same exports for operator/ctl `reserve-temporal admit --handoff JSON`. One-click re-admit (when hooked) posts those bytes through the same hook seam as a new admit — not resume-from-failed, not a silent stub. Guest does **not** call `runtime.apply compute-work`. Default guest does not invoke apply; opt-in lab may talk loopback ctl HTTP or subprocess `reserve-temporal` only.
- Echo / sleep / reserve demos still work. Default recorded catalog digest stays `sha256:77e9299f4b8ea4aeed46f71b91cc947d56e9bd169d795e70845123fef53d7e4e`.

## What is still thinner

- **Pause / resume full Temporal-backed UX.** Guest HTTP/UI pause/resume is thinner: durable-backed only (injected hook or opt-in lab adapter), stub refused honestly. Default hook stays inert — no guest→ctl HTTP over the mesh. Operators still use `python3 -m runtime.apply reserve-temporal pause|resume --id cw_…` for the ctl path, or set `PANORAMIX_CTL_HTTP` (loopback HTTP) / `PANORAMIX_RUNTIME_ROOT` (subprocess) for local-lab follow on the same hook. This does **not** stamp the #70 UX box.
- **Temporal-backed guest UX.** Guest UI status/cancel/pause/resume follow ctl when an opt-in hook is injected (`PANORAMIX_CTL_HTTP` preferred or `PANORAMIX_RUNTIME_ROOT`). Default hook stays inert (stub cancel + disabled stub pause; fail-closed without env). Still not Temporal-backed UX without a hook; not #70 Done, not cloud, not IFRS17.
- **Real chunk progress** / planner observations / parallelism visualization. Guest progress is reserve-temporal **path-slices** (four kernel stages when hooked) — **not** iec planner parallelism or iec SPA progress. Optional per-stage elapsed is thinner: runtime tip `9ba95bbb` / docs tip `5dc191cb` / main may supply `stages[].elapsed_ms`; omitted when timestamps are missing; never invented. Optional durable `wall_elapsed_ms` / `started_at` is thinner: runtime tip `9b6646e8` / main (PR #114) may supply it; shown only when the hook progress/status JSON includes it; omitted when missing; never invented; **not** a forecast; **not** IFRS17; **not** iec SPA.
- **Regulatory audit** product (job-scoped `/v1/audit/events`, six-month defensibility). Guest events prefer reserve-temporal JSONL when hooked (survives runtime restart); else process-memory. Kind filter + JSON/JSONL salvage export are thinner operator tools. Still not SIEM / compliance / iec `/v1/audit/events`.
- **SPA polish**: historical comparison is **match (thinner)** — guest history (in-process plus local lab files when persisted; same catalog / kind-class elapsed prefers durable `wall_elapsed_ms` when present, else created/updated; typical from priors when enough samples). History persistence is **match (thinner)** — optional wall persisted when seen; not a SIEM, not six-month audit, not a cross-host DB. Job list status filter + light auto-refresh is **match (thinner)** — real statuses only; opt-in or when `durable_hook` is active; stops on terminal; no invented progress. **Not** iec SPA historical widget, **not a forecast**, **not** prior-quarter IFRS17. Still no Slack, no live team directory, no data-catalog product, no full SPA framework. Guest investigate is catalog identity + static ownership tags on durable path-slices + event trail — **not** iec SPA / Slack / real data catalog.
- **IFRS17 / `reserve_ifrs17` math** and comparable **perf**. Guest now mirrors the third **parity** catalog from panoramix-runtime **main** (`runtime.reserve.parity_params` / `digest_for`); the stub still does not run that kernel. Faster stub wall time is not evidence. #70/#78 stay open.

## Catalogs

| Catalog | Guest today | Notes |
|---|---|---|
| recorded | yes | CI / default; digest `sha256:77e9299f4b8ea4aeed46f71b91cc947d56e9bd169d795e70845123fef53d7e4e` |
| live | yes | Heavier lab catalog; still not IFRS17 |
| parity (parity-scale) | yes | Same keys as recorded/live. Mirrors `runtime.reserve.parity_params()` / `digest_for` on panoramix-runtime **main** (`docs/reserve.md`). Digest `sha256:e180d2c2e3589b8762f92efa1bedb3d53ffeeb16648581ba13d537bcd3311102`. Still not IFRS17; not #70 Done |

## Done-when checklist for the #70 UX box

These are the [runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70) UX north-star items. **Unchecked items stay unchecked.** This guest PR does **not** mark #70 Done.

- [ ] Operator/actuary path for a representative SoS workflow is **match or better** than the corresponding iec-proto-c experience (clarity of job status, failure surface, recoverability, handoff docs)
- [ ] Side-by-side recorded against the named iec baseline **and** the #70 UX box stamped
- [ ] Pause / resume only when a real compute-plane durable path exists (not a guest stub) — guest HTTP is thinner/durable-only; default hook still inert; ctl `reserve-temporal pause|resume` is the operator path; opt-in lab adapter (loopback HTTP or subprocess) is the same hook seam, not a stub pretend
- [ ] Temporal-backed UX (guest UI status/cancel/pause/resume follows durable temporal-local runs without an injected hook) — operator/ctl `reserve-temporal` exists; opt-in lab still uses the hook injection point (`PANORAMIX_CTL_HTTP` or `PANORAMIX_RUNTIME_ROOT`); default remains inert stub cancel + disabled stub pause
- [ ] Real chunk / step progress (not stub stage metadata) — guest now prefers durable path-slices when hooked; still not iec planner / SPA progress
- [ ] Regulatory audit trail (not the local in-memory event list) — guest now prefers durable JSONL when hooked; still not SIEM / six-month / iec `/v1/audit/events`
- [ ] `north_star_done: true` (UX **and** perf). Perf is out of scope here
- [ ] Cloud unlock (#61 / #29) — **locked**; not this box

Guest-side progress that is **not** the #70 stamp:

- [x] Honest job detail panel on the operator UI
- [x] Honest thinner progress endpoint + named path-slice timeline (durable admit/project/fold/complete or hook-provided + ownership; stub stays stage i of n)
- [x] Optional path-slice stage elapsed (durable progress `stages[].elapsed_ms` from runtime tip `9ba95bbb` / docs tip `5dc191cb` / main, or event timestamps when honest; omit when missing; never invent; not iec planner)
- [x] Optional durable wall_elapsed_ms (runtime tip `9b6646e8` / main, PR #114; hook progress/status when present; omit when missing; never invent; not forecast / not IFRS17 / not iec SPA)
- [x] Compact handoff docs affordance (handoff + payload export, recoverability / re-admit, lab-compose reminders; not a second control plane)
- [x] Cancel confirm framing (cancel is not pause; durable cancel is ctl-mediated; no auto-retry; fail-closed without hook)
- [x] Hooked cancel + live status follow (ctl-mediated when durable; stub local; inert fail-closed)
- [x] Pause/Resume HTTP + UI (durable path only; stub `409 stub_only`; no pretend)
- [x] Event trail (durable JSONL when hooked, else process-memory; labeled not SIEM / not regulatory audit)
- [x] Thinner job-scoped events export / filter (kind filter + JSON/JSONL salvage; not SIEM / not regulatory defensibility)
- [x] Thinner investigate (catalog identity + static path-slice ownership tags when hooked; event trail on the same panel; not Slack / not data catalog / not SIEM)
- [x] Thinner historical-run comparison (guest process history plus local lab files when persisted; elapsed prefers durable `wall_elapsed_ms` when present else created/updated; typical/ETA only from real succeeded priors; not a forecast / not iec SPA widget)
- [x] Thinner history persistence (`.sos/jobs` / `PANORAMIX_SOS_JOBS_DIR`; reload across restart including persisted durable wall when seen; fail-closed if disabled; not SIEM / not six-month audit / not a cross-host DB)
- [x] Thinner job-list status filter + light auto-refresh (opt-in or durable_hook; stop on terminal; not a SPA framework)
- [x] Thinner failure / terminal summary (status + message + optional last events / stage; labeled not SIEM / not iec audit product)
- [x] Thinner recoverability (handoff + payload re-admit; one-click when durable hook is active; cancel/fail does not auto-retry; pause/resume remains durable-only)
- [x] One-shot lab compose (`scripts/lab_compose_reserve_temporal.py` / [docs/lab-compose.md](lab-compose.md); admit → cancel/fail → re-admit smoke; `--dry-run` in CI; pack_fill fragment for runtime #78 D; not #70 Done)
- [x] Thinner lab-compose pack-fill fragment (`tips.panoramix_runtime_tip` / `tips.guest_tip` when known; optional `durable.wall_elapsed_ms.panoramix` / `durable.stage_elapsed_ms.panoramix` only when measured from the hooked run; runtime tip `b81130f` / PR #120 / main; docs tip `63a168d` / PR #122 WSL assist-smoke stamp lineage; gap-report tip `84cb202` / PR #124; gap-report docs tip `aa4f09e` / PR #126 / main; merge tip `5086d0f` / PR #128 / main; merge docs tip `6ecb645` / PR #130 / main; apply-metrics tip `5d399f7` / PR #132 / main; apply-metrics docs tip `3904ee4` / PR #134 / main; apply-notes tip `48a8645` / PR #136 / main; apply-notes docs tip `6807509` / PR #138 / main; guest emit tip `b859466` / PR #52; guest gap-report tip `eb48605` / PR #60; guest merge tip `39064d5` / PR #64; guest apply-metrics tip `7c09f32` / PR #68; guest apply-notes tip `e5562c3` / PR #70; emit → `runtime.iec_parity_pack skeleton` via `--from-json` / flags; wall tip `9b6646e8` remains; omit when missing; never invent `metrics.wall_time_sec`; assist ≠ fill; does not write the live pack; #78 D assist only; not #70 UX Done / `north_star_done`)
- [x] Overnight assist smoke stamp is assist ≠ fill (`~/panoramix-lab/evidence-70/stamp-b81130f-iec-parity-pack/` named by runtime docs tip `63a168d` / PR #122; assist tip remains `b81130f` / #120; wall tip remains `9b6646e8`; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Overnight gap-report smoke stamp is gap-report ≠ Done / assist ≠ fill (`~/panoramix-lab/evidence-70/stamp-84cb202-iec-parity-gap-report/` named by runtime tip `84cb202` / PR #124; docs stamp `aa4f09e` / PR #126 / main; docs tip remains `63a168d` / #122; assist tip remains `b81130f` / #120; wall tip remains `9b6646e8`; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Lab-compose pack_fill.gap_report hint (`python3 -m runtime.iec_parity_pack gap-report` against the off-box pack after merge; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a copy-paste template; runtime tip `aa4f09e` / PR #126 / main; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; merge+gap-report pairing; does not run gap-report in CI; assist ≠ fill; gap-report ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Overnight merge assist smoke stamp is merge ≠ fill / merge ≠ Done (`~/panoramix-lab/evidence-70/stamp-5086d0f-iec-parity-merge/` named by runtime tip `5086d0f` / PR #128 / main; docs stamp `6ecb645` / PR #130 / main; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Lab-compose pack_fill.merge hint (`python3 -m runtime.iec_parity_pack merge … --from-json` against the off-box pack; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a copy-paste template; runtime tip `5086d0f` / PR #128 / main; merge docs tip `6ecb645` / PR #130 / main; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; merge+gap-report pairing; does not run merge in CI; merge ≠ fill; merge ≠ Done; assist ≠ Done; gap-report ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Overnight apply-metrics assist smoke stamp is apply-metrics ≠ fill / apply-metrics ≠ Done (`~/panoramix-lab/evidence-70/stamp-5d399f7-iec-parity-apply-metrics/` named by runtime tip `5d399f7` / PR #132 / main; docs stamp `3904ee4` / PR #134 / main; merge tip remains `5086d0f`; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; assist smoke ≠ fill; smoke walls ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Lab-compose pack_fill.apply_metrics hint (`python3 -m runtime.iec_parity_pack apply-metrics … --from-durable-panoramix` against the off-box pack; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a copy-paste template; runtime tip `5d399f7` / PR #132 / main; apply-metrics docs tip `3904ee4` / PR #134 / main; merge tip remains `5086d0f`; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; apply-metrics + merge + gap-report pairing; does not run apply-metrics in CI; does not supply wall numbers; apply-metrics ≠ fill; apply-metrics ≠ Done; assist ≠ Done; merge ≠ fill; merge ≠ Done; gap-report ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Overnight apply-notes assist smoke stamp is apply-notes ≠ fill / apply-notes ≠ Done (`~/panoramix-lab/evidence-70/stamp-48a8645-iec-parity-apply-notes/` named by runtime tip `48a8645` / PR #136 / main; docs stamp `6807509` / PR #138 / main; apply-metrics tip remains `5d399f7`; merge tip remains `5086d0f`; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; assist smoke ≠ fill; smoke notes ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Lab-compose pack_fill.apply_notes hint (`python3 -m runtime.iec_parity_pack apply-notes …` against the off-box pack; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a copy-paste template; runtime tip `48a8645` / PR #136 / main; apply-notes docs tip `6807509` / PR #138 / main; apply-metrics tip remains `5d399f7`; apply-metrics docs tip remains `3904ee4`; merge tip remains `5086d0f`; gap-report tip remains `84cb202`; wall tip remains `9b6646e8`; apply-notes + apply-metrics + merge + gap-report pairing; does not run apply-notes in CI; does not supply wall numbers; does not invent UX strings; apply-notes ≠ fill; apply-notes ≠ Done; assist ≠ Done; apply-metrics ≠ fill; apply-metrics ≠ Done; merge ≠ fill; merge ≠ Done; gap-report ≠ Done; does not write the live pack; #78 D still unfilled; not #70 UX Done / `north_star_done`)
- [x] Operator live #78 D pack is off-box (`~/panoramix-lab/evidence-70/iec-parity-live-20260912/` with STRICT `stamp-48a8645-iec-parity-live/`; live fill ≠ Done; `north_star_done` false; `comparable` false; does not unlock cloud)
- [x] Lab-compose opt-in catalog live|parity (alias parity-scale; `--catalog` / `LAB_COMPOSE_CATALOG`; default recorded so CI / `--dry-run` stay unchanged; parity digest `sha256:e180d2c2e3589b8762f92efa1bedb3d53ffeeb16648581ba13d537bcd3311102`; parity ≠ IFRS17 / ≠ ADSL; does not stamp `north_star_done`; does not unlock #61 / #29; does not invent walls or flip comparison flags)
- [x] Async durable admit / mid-flight poll (admit returns a running `cw_…` id; guest polls `GET /progress` / `GET /events`; `--catalog parity` is mid-flight poll + cancel, not wait-for-succeed; live|parity timeout is `ctl_admit_timeout` not stub / not lab-serve-down; depends on runtime #143 serve/CLI admit-return-running; does not stamp `north_star_done`; does not close runtime #70 / #78)
- [x] Thinner CTL_HTTP unreachable / lab-serve health affordance (`ctl_http_unreachable` when serve is down; fail closed; not a hung poll; not pretend durable)
- [x] Handoff / payload affordances (WorkHandoff emit only)
- [x] This side-by-side note
- [x] Mirror recorded / live / parity (parity-scale) catalog keys + parity digest (`runtime.reserve.parity_params` on main)

# Panoramix operational notes (sos guest)

Guest for [guypayeur/panoramix](https://github.com/guypayeur/panoramix). Pin **0.5**. Compute engines: [panoramix-runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70) Slice B. Hard reference: [`runtime/compute_work.py`](https://github.com/guypayeur/panoramix-runtime/blob/main/runtime/compute_work.py) on runtime main. Not #70 Done; not #78 Done; does not stamp `north_star_done`; does not unlock #61 / #29.

## Claim

This repo is a real guest origin (not under `platform-tools/fixtures/`). The platform owns the envelope; sos stays opaque domain code. Day-one is a **jobs HTTP API + operator UI** with an in-process stub runner — **not** iec-proto-c and **not** a compute plane.

## Guest compute seam (#70 Slice B; not Done)

The guest submits **opaque work** over HTTP (`POST /v0/jobs`) using the Slice B shape in [`runtime/compute_work.py`](https://github.com/guypayeur/panoramix-runtime/blob/main/runtime/compute_work.py) on panoramix-runtime main: `kind` (`job`|`stage`|`chunk`), `class` (`cpu`|`gpu`), `payload_digest` (`sha256:` + 64 hex). Status lifecycle is `queued` → `running` ⇄ `paused` → `succeeded` | `failed` | `canceled`. `paused` is non-terminal. List/get/cancel stay on the same public port (`GET /v0/jobs?status=` filters real statuses only). Pause/resume are durable-path only. Operator UI light auto-refresh is opt-in or when `durable_hook` is active; it stops on terminal and does not invent progress.

A **local-only** demo shortcut (`demo: echo|sleep|reserve` plus params) synthesizes that opaque shape so the operator UI does not require hand-computed digests. Stub runner metadata may nest under `local`; it is not a runtime handoff field. `local.backed` is `stub` (in-process fallback) unless an operator-injected hook admits the job (`runtime`).

`demo: "reserve"` is a **stub / UX seed** for operator submit → status → cancel beside the named iec baseline **`grammar/examples/reserve_ifrs17`**. Pause/resume is **not** claimed on this stub. Payload bytes **mirror** panoramix-runtime **main** helpers `runtime.reserve.recorded_params` / `runtime.reserve.live_params` / `runtime.reserve.parity_params` / `runtime.reserve.digest_for` ([`docs/reserve.md`](https://github.com/guypayeur/panoramix-runtime/blob/main/docs/reserve.md) / [`runtime/reserve.py`](https://github.com/guypayeur/panoramix-runtime/blob/main/runtime/reserve.py)): `workload`, `accounts`, `horizon`, `paths`, `seed`, `lapse_bps`, `discount_bps` — thinner than IFRS17; guest copies catalogs and does **not** import runtime. Recorded digest is `sha256:77e9299f4b8ea4aeed46f71b91cc947d56e9bd169d795e70845123fef53d7e4e`. Parity digest is `sha256:e180d2c2e3589b8762f92efa1bedb3d53ffeeb16648581ba13d537bcd3311102` (alias `parity-scale`). Optional `label` / `stages` / `seconds` are local stub UX only. **Not** IFRS17 math, **not** a copy of iec-proto-c, **not** a perf baseline until #83 + remeasure. This does **not** close #70.

### Operator / ctl seam (stub fallback vs binding)

Transport today is **operator/ctl-mediated only**. Compute mesh is **`from: compute-job` → `to: sos`** (worker calls Unit). The guest does **not** dial Ray / Temporal / AWS, does **not** open guest→ctl HTTP for compute-work, and does **not** call `runtime.apply compute-work`.

Do not invent `PLATFORM_RAY_*`, engine URLs, guest-callable ctl HTTP, or env that adds mesh destinations (`PLATFORM_MESH_*`). Operator/ctl:

1. **Stub fallback (default):** Guest UX `POST /v0/jobs` runs in-process.
2. **Export:** `GET /v0/jobs/{id}/handoff` (exactly `id`/`kind`/`class`/`payload_digest`/`status` — WorkHandoff projection, **no** nested `payload` key) and `GET /v0/jobs/{id}/payload` (`utf8`/`hex` of canonical JSON bytes). `GET /v0/jobs?status=` filters the list by real `job.status` (unknown values 400). Operator UX also exposes `GET /v0/jobs/{id}/progress` (durable reserve-temporal path-slices from panoramix-runtime **main** ctl `reserve-temporal progress --id` when a hook provides them: `stage` / `stages_total` / `stages_completed` / `fraction` / nested `progress`; optional per-stage `elapsed_ms` only when progress or durable event timestamps are real (runtime tip `9ba95bbb` / docs tip `5dc191cb` / main can supply `stages[].elapsed_ms`) — omitted when missing, never invented; optional `wall_elapsed_ms` / `started_at` when durable progress/status supplies it (runtime tip `9b6646e8` / main, PR #114) — omitted when missing, never invented; **not** a forecast; **not** IFRS17; **not** iec SPA; else stub stage metadata; **not** iec planner parallelism) and `GET /v0/jobs/{id}/events` (durable reserve-temporal JSONL from ctl `reserve-temporal events --id` when a hook provides them: `source: durable`, `events_durable`, `events_n`; store `.runtime/reserve-temporal/events/`; else process-memory; filter `?kind=admit,StageCompleted,…`; export `?format=jsonl` for local salvage; **not** a SIEM, **not** iec `/v1/audit/events`, **not** regulatory defensibility). Historical comparison: `GET /v0/jobs/{id}/compare` against recent same-catalog (or same kind/class) jobs in guest history (in-process plus local lab files under `.sos/jobs` / `PANORAMIX_SOS_JOBS_DIR` when persisted; fail-closed if disabled) — elapsed + optional typical/ETA from succeeded priors only; **not a forecast**, **not** IFRS17, **not** iec SPA historical widget. Investigate (thinner, on job + progress): catalog identity already on the job (name + short digest) for cross-check — **not** a data-catalog product; static path-slice ownership tags when hooked (`admit` / `project` / `fold` / `complete`) — **not** Slack. Failed/canceled jobs expose `terminal` (status + message + optional last events / stage; **not** a SIEM, **not** iec `/v1/audit/events`) and `recoverability` (handoff + payload for operator/ctl re-admit; one-click `POST /v0/jobs/{id}/re-admit` when a durable hook is active — a **new** admit, fail-closed without hook or payload, no silent stub; cancel/fail does **not** auto-retry). Job detail also exposes compact `handoff_docs` reminders (handoff + payload + re-admit + lab-compose) — not a second control plane. Pause/resume: `POST /v0/jobs/{id}/pause` and `POST /v0/jobs/{id}/resume` (durable path only; stub `409 stub_only`). See [docs/ux-side-by-side.md](docs/ux-side-by-side.md).
3. **Operator binding path:** operator/ctl admits that tuple on the runtime compute plane via the binding. Guest **never** POSTs to ctl and does **not** call `runtime.apply compute-work`. Binding examples: [`local-sos-compute.example.yaml`](https://github.com/guypayeur/panoramix-runtime/blob/main/bindings/local-sos-compute.example.yaml), [`local-reserve.example.yaml`](https://github.com/guypayeur/panoramix-runtime/blob/main/bindings/local-reserve.example.yaml) (Unit sos, port 18280, pin 0.5; engine kind in the binding only). Durable **temporal-local** reserve uses [`local-reserve-temporal.example.yaml`](https://github.com/guypayeur/panoramix-runtime/blob/main/bindings/local-reserve-temporal.example.yaml) (`engine.kind: temporal-local`, guest-sos pin 0.5; mesh `temporal-worker` → `sos`) on panoramix-runtime **main**. Ctl `reserve-temporal progress --id` exposes `stage` / `stages_total` / `stages_completed` / `fraction` / nested `progress` (verified @ `63c4d8e`); optional `stages[].elapsed_ms` when the tip supplies it (verified @ `9ba95bbb` / docs tip `5dc191cb` / main — omit when missing; never invent); optional `wall_elapsed_ms` / `started_at` when the tip supplies it (verified @ `9b6646e8` / main, PR #114 — omit when missing; never invent; not a forecast; not IFRS17; not iec SPA); `events --id` exposes append-only JSONL under `.runtime/reserve-temporal/events/` (`events` / `events_n` / `events_durable`; verified @ `d86552e`); also `pause|resume` (verified @ `3a164cd`); status `paused`.
4. **Opt-in lab loopback:** `PANORAMIX_CTL_HTTP` (preferred; fail closed if unset/non-loopback) injects `LabReserveTemporalHttpHook` and calls runtime loopback ctl HTTP on panoramix-runtime **main** @ `fb901542` ([`docs/reserve.md`](https://github.com/guypayeur/panoramix-runtime/blob/main/docs/reserve.md) § Loopback ctl HTTP; `POST/GET /reserve-temporal/…`, port **19215** on `local-reserve-temporal`). `PANORAMIX_RUNTIME_ROOT` still injects `LabReserveTemporalHook` and subprocesses `python3 -m runtime.apply reserve-temporal` locally. One-shot lab compose: `scripts/lab_compose_reserve_temporal.py` starts `runtime.serve` + guest `PANORAMIX_CTL_HTTP` + recorded reserve, then tears down (`--dry-run` for CI; see [docs/lab-compose.md](docs/lab-compose.md)). Not guest→ctl HTTP over the mesh. Not #70 / #78 Done.

```bash
python3 -m runtime.apply reserve-temporal admit --catalog recorded   # or live|parity; or --handoff JSON
python3 -m runtime.apply reserve-temporal status --id cw_…
python3 -m runtime.apply reserve-temporal progress --id cw_…
python3 -m runtime.apply reserve-temporal events --id cw_…
python3 -m runtime.apply reserve-temporal pause --id cw_…
python3 -m runtime.apply reserve-temporal resume --id cw_…
python3 -m runtime.apply reserve-temporal cancel --id cw_…
```

Guest flow stays UI / `POST /v0/jobs` with `demo:"reserve"` → export `GET /v0/jobs/{id}/handoff` (+ `/payload`) → operator/ctl admits via `reserve-temporal`. Guest-facing shape stays WorkHandoff only — no `workflow_id` / `task_queue` / `temporal:` on the seam or Unit YAML. Cancel on this path is **workflow cancel** (operator ctl). When a durable hook is active, guest cancel is ctl-mediated. Guest local cancel remains the stub path. Pause/resume on guest HTTP/UI is **durable-path only** (injected hook or opt-in lab adapter); stub jobs return `409 stub_only` and point at ctl `pause|resume`. Cancel is not pause.

`sos.runtime_hook.InertRuntimeHandoffHook` is inert: admit/cancel/status/pause/resume/progress/events no-op and the in-process stub is the fallback. The default hook stays inert (fail-closed without env). Cancel signals the hook first (same honesty as pause), then marks the guest job terminal if still live — or follows `hook.status()` when ctl already reports terminal. `GET /v0/jobs/{id}` and list refresh prefer `hook.status()` when `runtime_ref` exists (paused/running/terminal from ctl, not a stale stub clock). Do **not** invent guest→ctl HTTP over the mesh. Opt-in lab: `PANORAMIX_CTL_HTTP` (loopback origin, example `http://127.0.0.1:19215`) injects `LabReserveTemporalHttpHook` on the same seam and prefers durable status/progress/events/pause/resume/cancel via runtime.serve verbs — fail closed if unset or off loopback. When the origin is valid but `runtime.serve` is down (connection refused / timeout), admit/status/progress fail closed with `ctl_http_unreachable` (lab-serve down) — not a hung poll, not pretend durable. Optional `PANORAMIX_CTL_HTTP_BEARER` when `ctl.require` is on. `PANORAMIX_RUNTIME_ROOT` (usable panoramix-runtime checkout) still injects `LabReserveTemporalHook` and subprocesses `python3 -m runtime.apply reserve-temporal` locally — fail closed if unset or the root is missing. Guest `GET /progress` prefers `hook.progress()` when durable-backed. Guest `GET /events` prefers `hook.events()` JSONL when durable-backed. Does **not** close #70 / #78; cloud stays locked.

**Cancel:** cancel is not pause. Durable cancel is ctl-mediated. Stub-backed → local only. Runtime-backed (`PANORAMIX_CTL_HTTP` preferred or `PANORAMIX_RUNTIME_ROOT`) → signal the hook first, then mark the guest job `canceled` if it was still live (running or paused) or follow `hook.status()` when ctl already reports terminal. Fail-closed without hook (inert default). Cancel/fail does **not** auto-retry. Durable temporal-local cancel is operator/ctl `reserve-temporal cancel` (workflow cancel). Pause/resume require the durable path (`python3 -m runtime.apply reserve-temporal pause|resume --id cw_…`).

Runtime bindings will select engines later (slices C/D/E). Request bodies that smuggle engine brand keys or URL schemes are **400**. How bindings attach compute is documented on [runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70), not in `.platform/contract.yaml`. This alignment does **not** close #70 and does **not** unlock #61 / #29.

## Domain-leak log

| Temptation | Decision |
|---|---|
| Add `image:`, `ray:`, `temporal:`, or `aws:` to Unit/System YAML | **Rejected** — pin stays **0.5**; engines and image digests live in runtime bindings / lock sidecars |
| Lift iec-proto-c L1–L2 (or any iec tree) into this Git | **Rejected** — greenfield guest; iec is a north-star UX/perf **benchmark**, not a dependency |
| Put actuarial request/response schema types on the adapter | **Rejected** — guest speaks HTTP on the public port |
| Encode workflow IDs / cluster addresses in Unit Git | **Rejected** — [runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70) Slice B: submit `kind`/`class`/`payload_digest` without engine URLs/schemas in the contract |
| Put `temporal:` / `workflow_id` / `task_queue` on WorkHandoff, `POST /v0/jobs`, or Unit YAML | **Rejected** — seam is `kind`/`class`/`payload_digest` only; temporal-local lives in the runtime binding |
| Accept engine brand fields on `POST /v0/jobs` | **Rejected** — 400 `engine_smuggle`; engines stay in runtime bindings |
| Invent guest-callable ctl HTTP / mesh destinations / `PLATFORM_RAY_*` for loopback | **Rejected** — no guest→ctl HTTP over the mesh; no new mesh destinations |
| Opt-in lab `PANORAMIX_CTL_HTTP` loopback ctl HTTP to `reserve-temporal` on the existing hook seam | **Allowed (lab only)** — fail closed if unset/non-loopback; preferred over subprocess when set; default hook stays inert; not mesh ctl; not `compute-work`; not #70 / #78 Done; cloud locked |
| Pretend durable when `PANORAMIX_CTL_HTTP` is set but `runtime.serve` is down | **Rejected** — admit/status/progress fail closed with `ctl_http_unreachable` (lab-serve down); not a hung poll; not stub pretend-durable |
| One-shot lab compose (`scripts/lab_compose_reserve_temporal.py` / [docs/lab-compose.md](docs/lab-compose.md)) that starts `runtime.serve` + guest `CTL_HTTP` | **Allowed (lab only)** — `--dry-run` in CI (no Temporal); live path fail-closed without `PANORAMIX_RUNTIME_ROOT`; `pack_fill.fragment` may feed `runtime.iec_parity_pack skeleton` (tip `b81130f` / PR #120; docs tip `63a168d` / PR #122 WSL assist-smoke stamp lineage; gap-report tip `84cb202` / PR #124; gap-report docs tip `aa4f09e` / PR #126 / main; merge docs tip `6ecb645` / PR #130 / main; wall tip `9b6646e8`; measured durable only); `pack_fill.merge` hints `runtime.iec_parity_pack merge … --from-json` (tip `5086d0f` / PR #128 / main; merge docs tip `6ecb645` / PR #130 / main; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a template; does not run merge; merge ≠ fill; merge ≠ Done; assist ≠ Done); `pack_fill.apply_metrics` hints `runtime.iec_parity_pack apply-metrics … --from-durable-panoramix` (tip `5d399f7` / PR #132 / main; cd+cmd when `PANORAMIX_RUNTIME_ROOT` known, else a template; does not run apply-metrics; does not supply wall numbers; apply-metrics ≠ fill; apply-metrics ≠ Done; assist ≠ Done); `pack_fill.gap_report` hints `runtime.iec_parity_pack gap-report` (does not run gap-report; gap-report ≠ Done); not guest→mesh ctl; not SIEM; not IFRS17; pin 0.5; does not close #70 / #78; does not unlock #61 / #29; `north_star_done` false |
| Opt-in lab `PANORAMIX_RUNTIME_ROOT` subprocess to `reserve-temporal` on the existing hook seam | **Allowed (lab only)** — fail closed if unset/missing; default hook stays inert; not `compute-work`; not #70 / #78 Done; cloud locked |
| Add `PLATFORM_RAY_*` / invent `PLATFORM_COMPUTE_*` so “jobs can run for real” | **Rejected** — no guest-callable ctl HTTP; operator/ctl admits WorkHandoff; mesh is compute-job → sos |
| Env that adds mesh destinations (`PLATFORM_MESH_*`) | **Rejected** — mesh on local-sos-compute is already compute-job → sos; guest does not grow destinations |
| Guest→ctl HTTP for compute-work | **Rejected** — transport is operator/ctl-mediated only; guest emits WorkHandoff JSON |
| Put a nested `payload` field on `GET /v0/jobs/{id}/handoff` | **Rejected** — runtime `parse_work` refuses `payload` on submit; bytes live on `GET .../payload` as `utf8`/`hex` |
| Call `runtime.apply compute-work` from the guest | **Rejected** — guest emits WorkHandoff JSON only; mesh is compute-job → sos |
| Add a “SoS SDK” facet so apply understands the DSL | **Rejected** — HTTP/1.1 + `PLATFORM_*` env is the envelope |
| Teach `apply` to walk `sos/` imports so UI/job edits bump digest | **Rejected** — Rec 2 gotcha: digest is entrypoint paths only (`platform_run.py`). Entry may import `sos.http` (httpbin pattern); sibling `sos/` edits still must not be assumed to change emulate digest |
| Pin Flask/FastAPI/Ray on the Unit | **Rejected** — `build` is admission shape; this guest is stdlib; emulate does not execute `build.command` |
| Claim `demo: reserve` is IFRS17 math, iec `grammar/examples/reserve_ifrs17`, or a perf baseline | **Rejected** — UX seed stub only; named iec baseline stays on runtime `proofs/fixtures/iec-parity/method.yaml`; no perf baseline until runtime #83 + remeasure |
| Mark runtime #70 Done from this guest | **Rejected** — Slice B alignment is the opaque seam only |
| Claim `GET /v0/jobs/{id}/progress` is iec planner / SPA progress | **Rejected** — durable reserve-temporal path-slices when hooked, else stub `stage` / `stages_total`; not planner/chunk parallelism |
| Invent per-stage elapsed when progress/events timestamps are missing | **Rejected** — optional `elapsed_ms` only from durable progress or event timestamps; omit when missing |
| Invent job wall when hook omits `wall_elapsed_ms` | **Rejected** — optional durable wall only from hook progress/status JSON (runtime tip `9b6646e8` / main, PR #114); omit when missing; not a forecast; not IFRS17; not iec SPA |
| Treat handoff docs as a second control plane | **Rejected** — compact job-detail reminders for handoff + payload + re-admit + lab-compose; operator clarity only |
| Ship pause/resume that pretends the stub is Temporal | **Rejected** — stub-only pause/resume is `409 stub_only`; durable path (injected hook) or operator/ctl `reserve-temporal pause|resume`; cancel is not pause |
| Claim #70 / #78 Done or `north_star_done` because temporal-local ctl exists | **Rejected** — documenting `reserve-temporal` does not stamp those boxes; cloud #61 / #29 stay locked |
| Invent pack-fill `metrics.wall_time_sec` or stamp `north_star_done` from guest emit | **Rejected** — fragment feeds `runtime.iec_parity_pack skeleton` (runtime tip `b81130f` / PR #120; docs tip `63a168d` / PR #122 assist-smoke lineage; gap-report tip `84cb202` / PR #124; gap-report docs tip `aa4f09e` / PR #126 / main; merge docs tip `6ecb645` / PR #130 / main; apply-metrics tip `5d399f7` / PR #132 / main) with measured durable only; omit when missing; assist ≠ fill; overnight stamp is assist ≠ fill; overnight gap-report stamp is gap-report ≠ Done; overnight merge stamp is merge ≠ fill / merge ≠ Done; overnight apply-metrics stamp is apply-metrics ≠ fill / apply-metrics ≠ Done (assist smoke ≠ fill; smoke walls ≠ Done); apply-metrics / merge / gap-report hints do not run the CLI; #78 D stays open |
| Call the event trail a SIEM / regulatory audit / iec `/v1/audit/events` product | **Rejected** — durable reserve-temporal JSONL when hooked (survives runtime restart); else process-memory; kind filter + JSON/JSONL salvage are thinner operator tools; not SIEM, not six-month compliance, not regulatory defensibility |
| Claim investigate is iec SPA / Slack / a data-catalog product | **Rejected** — catalog identity already on the job (name + short digest) for cross-check; static path-slice owners when hooked; not Slack, not a live team directory, not a data catalog |
| Invent wall times / ETA without prior jobs in guest history | **Rejected** — compare prefers durable `wall_elapsed_ms` when the hook or a persisted job field includes it (runtime tip `9b6646e8` / main); else created/updated on list/get identity (in-process plus reloaded lab files); typical/ETA only from succeeded priors when two or more samples exist; empty/single-prior stay honest; omit when missing; never invent |
| Treat `.sos/jobs` / `PANORAMIX_SOS_JOBS_DIR` as a SIEM / six-month audit / cross-host DB | **Rejected** — local lab records for compare / recoverability only; fail-closed if disabled or unwritable; not SIEM; not IFRS17; not #70 Done |
| Pretend cancel/fail auto-retries or resume-from-failed | **Rejected** — recoverability is handoff + payload for operator/ctl `reserve-temporal admit --handoff JSON`; one-click re-admit is a **new** admit when a durable hook is active; pause/resume stays durable-only; stub `409 stub_only` |
| Silent stub re-admit / guest→mesh ctl from one-click recoverability | **Rejected** — `POST /v0/jobs/{id}/re-admit` fail-closes without `PANORAMIX_CTL_HTTP` / `PANORAMIX_RUNTIME_ROOT` or when payload is missing (`409 re_admit_unavailable`); not resume-from-failed; not SIEM; not IFRS17; not #70 Done |
| Claim day-one feature/UX/perf parity with iec-proto-c | **Rejected** — north star is UX/perf on agreed workflows ([runtime#70](https://github.com/guypayeur/panoramix-runtime/issues/70)); day-one is thinner |
| Ship a SPA framework / always-on invented progress for the job list | **Rejected** — stdlib HTML/JS; status filter is real `job.status` only; light auto-refresh is opt-in or `durable_hook`; stops on terminal; no invented progress |
| Mark the #70 UX box Done / `north_star_done` from this guest | **Rejected** — [docs/ux-side-by-side.md](docs/ux-side-by-side.md) walks the journey honestly; #70/#78 stay open |
| Unlock AWS/GKE/ECS from this guest | **Rejected** — local lab before AWS; cloud spend issues stay locked on the runtime |

## Digest gotcha

`run.entrypoint` names only `platform_run.py`. Editing `sos/jobs.py` (or other siblings) alone must **not** change the deploy digest under emulate’s entrypoint rule. Editing `platform_run.py` must. The thin entrypoint **may** import `sos.http` (like httpbin’s `platform_run` importing `httpbin.core.app`); that import does not expand the digest to the package.

## Engines

Do not add engine fields here. The container/compute runtime ([panoramix-runtime](https://github.com/guypayeur/panoramix-runtime)) records image digests and binding-selected engines **outside** this Unit.

## Run (with panoramix tools available)

```bash
GUEST=/path/to/panoramix-guest-sos
# from a panoramix checkout:
python3 platform-tools/platform_check.py "$GUEST"
python3 platform-tools/platform_emulate.py "$GUEST" --run --duration 2
python3 platform-tools/platform_serve.py "$GUEST" --port 19220
```

Emulate only: Unix adapter, localhost edge, stamped `PLATFORM_NETWORK_EGRESS`. `build.command` is not run. Still not `process` / `container` / `microvm`.

## Image digest (container profile, later)

If a container runtime admits a guest-CI image digest, that pin is **not** a Unit field. Pin stays **0.5**. Do not add `image:` here.
